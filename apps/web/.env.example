# Server-side rewrite target for same-origin browser API requests.
API_PROXY_TARGET=http://localhost:4000
# Backwards-compatible public fallback. It is an origin, never a credential.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
